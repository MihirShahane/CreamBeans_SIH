# Set to "false" to call the real FastAPI backend instead of mock data.
VITE_USE_MOCK_API=true

# Base URL of the FastAPI backend (only used when VITE_USE_MOCK_API=false).
VITE_API_BASE_URL=http://localhost:8000
